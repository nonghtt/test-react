# 학습 기록

## 현재 상태

- **진행 중인 실습**: `exercises/02-state-and-events.md`
- **마지막 리뷰 커밋**: `c670d89` (02 1차) — 2차 리뷰는 미커밋 작업 트리 기준
- **다음 할 일**: 02 통과 확정(2026-09-21). `CartItems`의 `key={item.id}` 수정과 소계 회귀 복구를 확인함. 진행 중인 리팩토링 잔여: `Form.jsx` 최상단 죽은 주석 블록 · `CartItems`에 안 쓰는 `totalPay` prop 전달 · `alert`+`alertMenus` 합치기 · `alert` 이름 · `initializeCart` 이름 · `getQtyByMenuId`의 `Object.values().find()` · `"help false"` 클래스. 정리되면 `feat(02): 보완` 커밋 후 03 출제.
- **02 0단계 설계(확인됨)**: 메뉴는 상수 / 장바구니 `[{ id, qty }]` state, 상위 컴포넌트에서 관리 / 요청사항 문자열 state / 주문 완료 알림 state / 총액·소계·총 수량·`N개 담김`은 계산. 처음엔 총액을 state로, 알림을 "리렌더링 되면 안 되니 state 아님"으로 답했다가 질문으로 유도 후 수정 — 리뷰 때 실제 코드가 이 설계대로인지 대조.
- 학습자는 Vue 경험이 있음 (children을 slot에 비유) — Vue 비유로 설명하면 빠름.
- 갱신: 2026-09-21

세션이 새로 시작되면 이 블록만 보면 됩니다. 실습이 끝날 때마다 Claude가 갱신합니다.

---

각 실습이 끝날 때마다 Claude가 이 파일에 기록합니다. 로드맵과 개념 체크리스트는 [ROADMAP.md](./ROADMAP.md).

기록 형식:
- **결과**: 통과 / 보완 후 통과 / 보류
- **잘한 점**: 다음에도 유지할 것
- **개선점**: 개념 오해 · 설계 · 관례 순
- **약점 메모**: 다음 단계나 실제 프로젝트에서 다시 확인할 것

---

## 누적 약점 목록

실제 프로젝트에서 Claude가 우선 확인할 항목. 해결되면 취소선 처리.

- 변하지 않는 값을 `useState`에 넣음 — "state = 시간에 따라 변하는 값" 기준이 아직 자리 잡지 않음 (00)
- 스타터(샘플 화면)의 숫자·내용에 결과를 맞추려 함 — 요구사항과 데이터가 기준 (00)
- `key`를 "고유한 값을 넣는 곳" 정도로만 이해 — index가 문제 되는 경우(추가·삭제·정렬 시 state가 엉뚱한 항목에 붙음)를 설명하지 못함 (00)
- prop 이름 불일치 실수 — 부모가 넘긴 이름과 자식이 받는 이름 (00, 2회)
- 완성 조건을 하나씩 대조하지 않고 "완료" 보고 — 01 1차에서 11개 중 5개 미충족, 02 1차에서 12개 전부 [x] 체크했으나 5개 미충족 (01, 02)
- 범용 컴포넌트에 특정 도메인 지식을 넣음 — `Badge`가 프로젝트 status를 앎 (01)
- 에러 원인을 모른 채 `?.`로 덮음 — 값이 예상과 다르면 먼저 `console.log` (01)
- early return(`if (...) return null;`)이 손에 익지 않음 — JSX 안 삼항으로만 분기하려 함 (01)
- 래퍼(컨테이너) 요소를 `map` 안팎 어디에 둘지 헷갈림 — "목록에 1개 vs 항목마다 1개" 기준 (01, 3회)
- ~~`key`를 "고유한 값을 넣는 곳" 정도로만 이해~~ — 01 질문 4번에서 정확히 답변 (해결)
- ~~`key={index}` 습관~~ — 02 2차에서 `item.id`로 수정, 이유도 설명 (해결)
- 중간 리뷰 지적 사항을 다음 보고 전에 다시 훑지 않음 — 02 중간 리뷰 6개 중 3개(index key, price 포맷, main.jsx) 미반영 (02)

---

## 실습 기록

### 00 첫 컴포넌트 — ✅ 보완 후 통과 (2026-09-17)

- 시작: 2026-09-17

#### 1차 리뷰 (2026-09-17, `9bae5b2`) — 보완 요청

- **진행 중 막힌 곳**: `useState`를 컴포넌트 밖(모듈 최상위)에서 호출 → Invalid hook call. prop 이름 불일치 2회(`members`/`member`, `onClick`/`selectMember`). 선택을 "단일 선택"으로 읽고 state를 App에 올리려 했음 → 요구사항 재확인 후 카드 내부 state로 결정.
- **잘한 점**: `key={member.id}`, 삼항으로 배지를 DOM에서 제거, 카드별 state로 독립 토글, className·버튼 글자까지 선택 상태 반영. lint 통과. 자식→부모 콜백에 id를 올리는 패턴을 스스로 찾음(최종 코드에선 불필요해져 제거 — 제거한 판단도 좋음).
- **보완 항목 (통과 조건)**:
  1. `teamRoles` 필터로 PM·QA가 빠져 5명 중 3명만 렌더링됨 (요구사항 2·4)
  2. `useState(members)` — setter를 쓰지 않는 state
  3. 생각해볼 질문 3개 답변 미제출
- **관례 (통과와 무관)**: `let [selected, ...]` → 왜 `const`여야 하는지, 폴더명 `Components` vs 요구된 `components`(대소문자 구분 OS에서 문제), 커밋 메시지에 설명 누락(`feat(00): ...`), `Starter00.jsx`가 포매터로 변경됨(참고용 파일은 그대로 두기), 버튼 글자 `선택해제` vs 스타터 `선택 해제`.
- **나중에**: `setSelected(!selected)` vs 업데이터 함수 형태 — state 단계에서 다룸.

#### 2차 리뷰 (2026-09-17, `ebcba6f`) — 통과

- **보완 결과**: 역할 필터 제거(스타터의 "3명"을 요구사항으로 오해했던 것), `useState(members)` 제거 후 상수 직접 사용, 폴더명 `components`, 버튼 글자 수정. lint 통과. 폴더 대소문자 변경이 git에 반영되지 않아 Claude가 `git mv`로 처리하고 커밋.
- **질문 답변**: ① state 위치 — 답변은 없었으나 대화에서 단일 선택/독립 토글을 구분해 카드 내부로 결정함. ② key — "index도 동작하지만 데이터가 가진 값을 썼다". index가 위험한 경우는 답하지 못함 → 약점 목록. ③ 객체 통째 전달 — 가독성 때문, 필요한 속성이 적으면 개별 전달. 타당.
- **남은 것**: `let [selected, setSelected]` 그대로 — `const`인 이유는 02(state는 스냅샷)에서 다시 확인.
- **약점 메모**: 미래 요구사항(API)을 미리 코드에 반영하려는 경향. 의도는 좋으나 지금 요구사항 기준으로 가장 단순한 코드를 먼저.

### 01 컴포넌트와 props — ✅ 보완 후 통과 (2026-09-18)

- 시작: 2026-09-17
- 출제 의도: state 없이 props 설계에 집중. `Badge`/`Section`은 사용 모양을 고정해 prop 이름을 맞추는 연습(00 약점), 나머지는 직접 설계. `count && …`의 `0` 함정, `AvatarGroup`의 early return `null`, 팀→프로젝트→태그/담당자 중첩 `key`. 스타터 오해 방지 문구를 실습과 스타터 양쪽에 명시. 질문 4번은 00의 `key` 숙제.

#### 1차 리뷰 (2026-09-17, `64bff56`) — 보완 요청

- **진행 중 막힌 곳**: ① 팀을 `find`로 하나씩 꺼내 `<Section>` 3번 하드코딩(복붙 버그 발생) → `map`/`reduce`로 전환. ② `projects.tag?.map` — 배열에 `.tag` 접근 + 필드명 오타 + `return` 누락이 `?.`에 가려져 무증상. ③ `grid`/`avatar-group` 래퍼를 반복되는 컴포넌트 안에 넣어 레이아웃 깨짐("목록에 1개 vs 항목마다 1개" 기준 설명, CSS 문제라 사용자 요청으로 답을 알려줌). 커밋은 Claude가 대신 함.
- **잘한 점**: `reduce`로 합계, 중첩 `map`과 `key`(team.id / project.id / tag / member.id), status→라벨·색을 조회 객체로 정리, `ProjectCard`를 카드 1장 책임으로 축소, state 없음, lint 통과.
- **보완 항목 (통과 조건)**:
  1. `Badge` · `Section`이 고정된 사용 모양이 아님 — `children`을 한 번도 쓰지 않음(이번 실습의 핵심). `Section`이 team·ProjectCard를 알고 있음.
  2. `Badge`가 프로젝트 status를 앎 — 태그 이름이 `done`이면 상태 배지로 바뀌는 버그. 변환은 호출하는 쪽 책임.
  3. `총 0개` — `reduce`가 주석 처리되고 0으로 하드코딩됨.
  4. 데이터 팀에 `0개` 표시됨(요구 6), 빈 상태 상자가 `grid` 안에 있어 좁아짐(요구 7).
  5. 설명 없는 카드에 빈 `<p>`, 태그 없는 카드에 빈 `tag-list`가 DOM에 남음(요구 9).
  6. `AvatarGroup`이 member 1명을 받음 — 그룹이 담당자마다 생겨 겹침 없음, 요구 10(내부에서 `null` 반환) 구현 불가. 앞서 CSS 질문에서 설명했으나 미반영.
- **질문 답변**: ① children=Vue slot, 껍데기 재사용 — 이해 정확(단 코드에 미적용). ② `0 && …`가 화면에 `0`을 그린다는 점을 모름 — 직접 실험 필요. ③ "status는 Badge 안에" — 설계 재고 필요(보완 2). ④ index key면 선택이 앞 사람 카드에 붙는다 — 정답. 00의 `key` 숙제 해결.
- **관례**: `<section className="stack">` 위치(팀마다 1개), "마감" 글자 누락, 변하지 않는 값에 `let`, 불필요한 프래그먼트 `<>…</>`, 주석 처리된 코드.

#### 2차 리뷰 (2026-09-18, `a127ec8`) — 통과

- **진행 과정** (세션 2일차, 질문 중심으로 진행): `children`을 문서로는 이해 못 하다가 "태그 사이 내용 = `children`이라는 이름의 prop, Vue `<slot/>` 자리에 `{children}`"으로 정리 → `Badge`를 `tone` + `children`으로 재작성 → "부모가 자식이 받는 값을 알아야 하는 게 맞나" 질문 → 컴포넌트 인터페이스 개념(HTML `type` 속성 비유) → `Section`이 `team`이 아니라 `title`/`count`/`children`을 받는 이유 → "껍데기뿐인 컴포넌트가 의미 있나" → 레이아웃 컴포넌트와 분리 기준 → `AvatarGroup` early return에서 세 번 왕복(삼항→호출부 필터→`map` 안에 div→최종).
- **최종 코드**: `Badge`(tone 4종만 앎, 모르는 값은 기본 배지), `Section`(`<section>` 껍데기 + `count > 0` 규칙 + `children`), `ProjectCard`(status→`{label, tone}` 조회 객체, 설명·태그·마감 조건부), `AvatarGroup`(`members` 배열, 없으면 반환 없음, 그룹 div 1개). 완성 조건 11개 충족, lint 통과.
- **질문 답변**: ① `children` = slot, 껍데기 재사용 — 구현으로 확인. ② `&&`는 boolean이 아닌 왼쪽 값을 반환하므로 `0`이 그려짐 → 왼쪽을 boolean으로. ③ status 지식은 한 곳(`ProjectCard`의 조회 객체)에, `Badge`는 `tone`만 — "상태값의 집중화와 자식 컴포넌트 분리 기준을 배웠다". ④ 어제 정답.
- **남은 관례** (통과와 무관): `return;` → `return null;`, `return` 뒤 불필요한 `else`, `statusConfig`가 컴포넌트 함수 안(렌더마다 재생성 — 05 렌더링 단계에서 다룸), 닫는 태그 `></X>` 대신 `<X />`.
- **약점 메모**: early return 패턴이 손에 익지 않음(설명 3회 후 성공). 구조 하나를 고치면 다른 구조가 되돌아가는 일이 잦음(래퍼 위치) — 고친 뒤 이전 지적 사항을 다시 훑는 습관 필요. 개념은 대화로 빠르게 잡지만 코드로 옮기는 단계에서 왕복이 많음 — 02부터는 "먼저 구조를 말로 설명하게 한 뒤 구현" 방식 시도.

### 02 state와 이벤트 — ✅ 보완 후 통과 (2026-09-21)

- 시작: 2026-09-18
- 출제 의도: 카페 주문(메뉴 담기 → 장바구니 수량 조절 → 요청사항 → 주문). state 2~3개(장바구니 배열, 요청사항 문자열, 주문 완료 알림)를 App에 두고, 소계·합계·총 수량·`N개 담김`은 파생 값. 배열 안 객체 수량 변경(불변성), 0이면 filter, 제어 textarea + 글자 수 검증, submit + preventDefault. 실험 11번으로 "state는 스냅샷"을 직접 확인. 01 약점 반영: 0단계 "코딩 전 설계를 말로" 필수, `Badge` 재사용, early return 재확인(빈 장바구니), `let` 금지 조건 명시.

#### 1차 리뷰 (2026-09-18, 미커밋 작업 트리) — 보완 요청

- **진행 중 막힌 곳**: ① 0단계에서 합계를 state로, 알림을 "리렌더링되면 안 되니 state 아님"으로 답함 → 질문으로 수정. ② `onSelect({ menu })` 축약 객체 때문에 부모가 `item.menu`로 받음 — JS 문법 문제였음. ③ 중간 리뷰에서 `key={index}` 재등장, cart 모양 불일치(App 객체 / CartItems 배열), `function CartItems(cart)` 지적.
- **잘한 점**: `setCart(prev => …)` 업데이터 형태 일관 사용, 스프레드·구조분해 rest(`const { [id]: _, ...rest }`)로 불변 삭제, 객체 맵 `{ [id]: {…} }`로 중복 줄 방지, 소계·총 수량·합계 전부 계산, `CartItems` 빈 상태 early return 성공(01 약점 해소), 제어 textarea + `preventDefault`, 알림 수량·금액을 초기화 직전에 스냅샷으로 저장한 판단. lint 통과.
- **보완 항목 (통과 조건)**:
  1. `N개 담김` 배지 미구현 — 요구 2. `Badge` 재사용도 따라서 미충족. 완성 조건에는 [x]로 체크됨.
  2. 주문 버튼이 장바구니 비었을 때 활성 — 요구 8. 빈 장바구니로 주문하면 "0개 항목, 0원" 알림.
  3. 주문 후 요청사항이 비워지지 않음 — 요구 9. `text` state가 `Form` 안에 있어 `initializeCart`가 닿지 못함.
  4. 알림이 다음 담기에 사라지지 않음 — 요구 10. 게다가 `setAlert(!alert)` 토글이라 두 번째 주문에서 알림이 꺼짐.
  5. `let [alert, setAlert]` — 완성 조건 위반, [x]로 체크됨. 00·01에 이어 3회째.
  6. `key={index}` 2곳(메뉴, 장바구니) — 01 질문 4번에서 해결한 개념이 코드에서 되돌아감. 중간 리뷰 지적 후에도 미수정.
- **질문 답변**: ① "리렌더링 전에 콘솔이 실행돼서" — 타이밍으로만 이해. 그 렌더의 `cart` 변수 자체가 고정값이라는 점(스냅샷)까지는 못 감 → 재질문. ② name·price를 표시·계산 때문에 복사 — 이유 타당. 객체 맵을 고른 이유는 미답. ③ "리렌더링이 발생" — 오답. 계산 값도 리렌더링은 똑같이 됨. 진짜 위험은 갱신 누락·스냅샷 값으로 계산해 한 박자 늦는 것 — ①과 연결해 재질문. ④ 질문을 스프레드로 오독. 업데이터 함수 vs 직접 값 차이는 미답 → "모르겠다" 허용.
- **관례 (통과와 무관)**: `alert` 이름이 `window.alert`와 겹침. `alert` + `alertMenus` 두 state → 하나(`null` 또는 `{ qty, pay }`)로 합칠 수 있음. `totalPay`를 App과 Form에서 중복 계산. `addItem`이 `addToCart`를 그대로 감쌈. `initializeCart`가 알림까지 켬(이름과 역할 불일치). `` `help ${x && "error"}` ``가 `"help false"`를 만듦(01 질문 2 `&&` 함정의 변형). 카드 가격 `4500` 미포맷. 라벨 "(초과상태)"는 스타터의 샘플 구분용 문구. `src/main.jsx` 포매터 변경 미복구. 완성 조건 12개 전부 [x]인데 실제 5개 미충족 — 01 1차와 같은 패턴.


#### 2차 리뷰 (2026-09-21, 미커밋 작업 트리) — 통과

- **[정정 2026-09-21] 이 2차 리뷰의 "통과" 판정은 성급했음.** Claude가 `App.jsx`만 확인하고 `key={index}`가 두 곳 모두 고쳐진 것으로 기록했으나 `CartItems.jsx`는 그대로였음(그 파일은 애초에 수정되지 않았음 — `git status`의 변경 파일 목록을 보고도 놓침). 리뷰 때는 지적한 파일 전부를 다시 열 것. 아래 "보완 결과"는 이 오류를 포함한 기록.
- **보완 결과**: 6개 중 5개 반영. `Badge`를 `Card`의 children으로 넘겨 `N개 담김` 구현(01 부품 재사용 확인), 빈 장바구니 시 버튼 비활성, `initializeForm`으로 요청사항 초기화, `setAlert(true)`/`setAlert(false)`로 토글 제거, `const`, `key={item.id}`. lint 통과.
- **진행 중 막힌 곳**: 알림 state를 `let isAlert = false` 평범한 지역 변수로 바꿨다가 동작 안 함 → 1차 피드백의 "`let` → `const`"를 "state를 쓰지 말라"로 오해한 것. 본인 진단("렌더링마다 false로 초기화")은 정확했고, `useRef`를 후보로 떠올렸으나 "리렌더링을 일으키지 않는다"는 점을 짚어 주자 state로 복귀.
- **질문 답변**: ① `setTimeout(() => console.log(cart), 3000)` 실험을 직접 수행 → 3초 뒤에도 빈 객체. 이유는 스스로 못 찾아 설명함(핸들러는 그 렌더의 `cart` 바인딩을 가둔 클로저). 본인이 "알림 금액은 정확히 나온다"고 관찰한 것이 같은 원리의 반대 사례라 이를 지렛대로 설명. Vue의 반응형 객체(하나를 계속 수정) vs React(렌더마다 새 값) 비교가 먹혔음. ② 객체 맵을 고른 이유 — "수량 계산이 편해서, 대신 `Object.*`를 많이 쓰게 됨". 트레이드오프를 스스로 관찰한 점이 좋음. ③ 정답 — "리렌더링 전에 계산되어 방금 추가한 값이 유실된다"(한 박자 늦는 버그). 1차의 오답에서 교정됨. ④ 정답 — "index면 순서가 기준이라 앞 항목에 하이라이트가 붙는다"(01 답변 재현).
- **리팩토링 후보 (통과와 무관, 사용자가 선택)**: `alert`+`alertMenus` 두 state → 하나(`null` | `{qty,pay}`) · `alert` 이름이 `window.alert`와 겹침 · `totalPay`를 App·Form에서 중복 계산 · `getQtyByMenuId`가 id 키 맵을 두고 `Object.values().find()` · `addItem`이 `addToCart`를 그대로 감쌈 · `initializeCart` 이름과 역할 불일치(알림까지 켬) · `` `help ${isOverLimit && "error"}` ``가 `"help false"` 생성 · `Card`의 children 삼항이 `card-title`을 두 번 씀 · 카드 가격 미포맷 · 라벨 "(초과상태)"는 스타터의 샘플 구분 문구.
- **약점 메모**: 피드백을 반대 방향으로 실행하는 일이 있음(관례 지적 → 구조를 제거). 피드백이 "무엇을 바꾸라"인지 "무엇을 버리라"인지 애매하면 되묻도록 유도할 것. 실험을 시키면 실제로 해 보고 결과를 정확히 보고함 — 개념 질문은 앞으로도 실험 과제로 주는 편이 효과적.
